#include <iostream>
using namespace std;

int main(){
    int N, arreglo[10], sum = 0; 
    cin >> N; 
    for(int i = 0; i < N; i++){
        cin >> arreglo[i];
    }
    for(int i = 0; i < N; i++){
        sum+=arreglo[i];
    }
    cout << sum; 

    return 0;
}
